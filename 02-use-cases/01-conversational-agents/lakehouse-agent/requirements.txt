# Health Lakehouse Agent - Main Requirements

# Core dependencies
boto3>=1.34.0
bedrock-agentcore>=1.0.0
bedrock-agentcore-starter-toolkit>=0.2.6
strands-agents>=1.0.0

# Web UI
streamlit>=1.30.0
requests>=2.31.0

# MCP
mcp>=1.9.0

# Reader path — needed so a single venv runs the notebooks + the app:
# opensearch-py + aws-requests-auth: notebook 07 queries AOSS with SigV4-signed requests
# PyJWT: offline JWT decode (07 isolation checks; Streamlit id_token display)
# nest_asyncio: nested event loops for notebooks (asyncio.run inside Jupyter's running loop)
opensearch-py>=3.2.0
aws-requests-auth>=0.4.3
PyJWT>=2.8.0
nest_asyncio>=1.6.0

# JWT handling
python-jose[cryptography]>=3.4.0
cryptography>=41.0.0

# Development
jupyter>=1.0.0
ipykernel>=6.29.0

# Testing
pytest>=7.4.0
hypothesis>=6.92.0

streamlit>=1.30.0
requests>=2.31.0
boto3>=1.43.0

# Okta path: Authorization Code + PKCE is implemented directly in the app, using
# only the standard library plus requests, so no OAuth component is needed. PyJWT
# decodes the id_token for display only. Inert on the Cognito path.
PyJWT>=2.8.0

# MCP client for the shared "Tools available to you" panel (queries GW1's
# tools/list to show the per-persona, response-interceptor-filtered tool set).
# nest_asyncio supports running the async MCP client from Streamlit.
mcp>=1.9.0
nest_asyncio>=1.6.0
